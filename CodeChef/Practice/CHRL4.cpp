#include <iostream>
#include <vector>
#include <map>
#include <list>
#define ll long long

using namespace std;

int main()
{
    ll N, K;
    cin >> N >> K;
    ll specialNo[N];
    for(int i = 0; i <N; i++)
    {
        ll input;
        cin >> input;
        specialNo[i] = input;
    }
    
    
}